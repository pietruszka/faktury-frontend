import React, { Component } from 'react';

import { ContentContainer } from './Expenses_style'

export default class Expenses extends Component {
  render() {
      return(
        <ContentContainer>
          <h1>Wydatki</h1>
        </ContentContainer>
      );
 }
} 